# Server
server.port=${PORT:8080}
server.forward-headers-strategy=framework

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/field_service_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=root
spring.datasource.password=MyNewPassword123!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT
jwt.secret=thisIsExactlyThirtyTwoCharacters!!
jwt.expiration-minutes=120

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5174}

# Cloudinary
cloudinary.cloud-name=teylswh8
cloudinary.api-key=514281742524676
cloudinary.api-secret=EnzYYntrG8JteXrPI4h_pwFDAb0

# Upload limits
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# Production-friendly logging
logging.level.root=${LOG_LEVEL:INFO}
